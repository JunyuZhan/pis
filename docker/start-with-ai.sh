#!/bin/bash
# =============================================================================
# 说明：标准「仅镜像」部署栈不再包含 AI 容器（与群晖式单 compose 一致）。
# 若需人脸识别，请等待发布独立 AI 镜像后，自行在 compose 中增加服务或联系维护方。
# =============================================================================

set -e

RED='\033[0;31m'
CYAN='\033[0;36m'
YELLOW='\033[1;33m'
NC='\033[0m'

echo -e "${CYAN}========================================${NC}"
echo -e "${CYAN}  PIS / AI 服务${NC}"
echo -e "${CYAN}========================================${NC}"
echo ""
echo -e "${YELLOW}当前 docker-compose.yml 为「仅拉取预构建镜像」部署，不包含 AI 服务。${NC}"
echo -e "${YELLOW}请使用源码仓库在开发机构建 AI 镜像，或关注后续是否提供独立 AI 镜像与文档。${NC}"
echo ""
echo -e "${CYAN}启动主栈（不含 AI）：${NC}"
echo "  cd docker && docker compose pull && docker compose up -d"
echo ""
exit 0
